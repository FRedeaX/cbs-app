/**
 * Инкрементная статическая регенерация (ISR)
 */
export const POST = 600;
/**
 * Инкрементная статическая регенерация (ISR)
 */
export const PAGE = 600;

export const PREVIEW_POLL_INTERVAL = 10000;
